
- name: "My dataset 1"

  model:

    filePathList:
      - "./advanced_gundam_example/inputs/datasets/mydataset.root"

    tree: "events"

    selectionCutFormula: "selection != -1"

    nominalWeightFormula:
      - "asimovWeight"

